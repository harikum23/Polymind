# AgenticFlow Instructions

> Generated by AgenticFlow prep. Re-running prep refreshes the generated metadata below and preserves the editable sections between the marker comments.

## How AgenticFlow uses this file
- AgenticFlow stores a provider-agnostic instruction bundle in SQLite for this project.
- The locked security baseline is bundled inside AgenticFlow and refreshed into SQLite on every prep and goal run.
- Use this file only for project-specific additions; it cannot weaken the locked baseline.

## Generated Project Snapshot
- Project: polymind
- Path: `/Users/harishkumargundameedi/Projects/services/polymind`
- Detected stacks: docker, java, spring-boot
- Root entries: `Caddyfile`, `Dockerfile`, `README.md`, `build.gradle`, `build/`, `docker-compose.yml`, `docs/`, `gradle/`, `gradlew`, `gradlew.bat`, `searxng/`, `settings.gradle`
- Dependency hints: none
- Sample files: `Caddyfile`, `Dockerfile`, `README.md`, `build.gradle`, `docker-compose.yml`, `docs/ARCHITECTURE.md`, `docs/future-pending.md`, `gradle/wrapper/gradle-wrapper.jar`, `gradle/wrapper/gradle-wrapper.properties`, `gradlew`, `gradlew.bat`, `searxng/settings.yml`

## Common Commands
- build: ./gradlew build
- test: ./gradlew test

## Stack Notes
- Build/test through Maven or Gradle as configured; don't bypass the build tool.

## Editable Security Additions
> Add stricter project-specific security constraints here. These augment the bundled locked rules.
<!-- AGENTICFLOW:SECURITY:BEGIN -->
- Add stricter project-specific security rules here.
- Example: require review for changes under billing/ or auth/.
<!-- AGENTICFLOW:SECURITY:END -->

## Editable Special Instructions
> Add project-specific implementation notes, workflow reminders, or architecture constraints here.
<!-- AGENTICFLOW:SPECIAL:BEGIN -->
- Add project-specific implementation notes, workflow constraints, or architecture reminders here.
- Example: prefer existing repo scripts over ad-hoc commands.
<!-- AGENTICFLOW:SPECIAL:END -->
