/**
 * routing — the smart switch. Task classification, model registry, capability scoring,
 * forced-model / forced-category override. Selection precedence: explicit id, category alias, auto.
 */
@org.springframework.modulith.ApplicationModule
package com.polymind.routing;
