/**
 * inference — {@code Engine} port + adapters (Ollama/vLLM/llama.cpp/remote).
 * Streaming token relay over JDK 21 HttpClient (HTTP/2). Foundational module: depends on nothing.
 */
@org.springframework.modulith.ApplicationModule
package com.polymind.inference;
